<?php
declare(strict_types=1);

// Hook0 SDK utility: feature_hook

class Hook0FeatureHook
{
    public static function call(Hook0Context $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
