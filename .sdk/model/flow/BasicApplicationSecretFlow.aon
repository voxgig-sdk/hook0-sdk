# BasicApplicationSecretFlow

main: kit: flow: BasicApplicationSecretFlow:
{
  entity: "application_secret",
  kind: "basic",
  name: "BasicApplicationSecretFlow",
  step: [
    {
      input: {
        ref: "application_secret_ref01"
      }

      op: "create"
    }

    {
      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "application_secret_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "application_secret_ref01",
        srcdatavar: "application_secret_ref01_data",
        suffix: "_up0",
        textfield: "application_id"
      }

      op: "update",
      spec: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-application_secret_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicApplicationSecretFlow"
}